# Language

- 优先使用中文回复用户

# Shell Env

- You are running in Window 11 on WezTerm terminal, and use Git-Bash as shell
- 执行 shell 脚本之前先阅读下脚本内容
- 注意根据环境区分文件路径风格，举例：
  * Git-Bash: `/d/works/`
  * Windows: `D:\works\`

# CRITICAL Rules

- MUST use `rg` instead of `grep`
- MUST use `fd` instead of `find`
- MUST read every file you modify in full before editing.
- DO NOT edit too much code lines that human cannot review.

